"""Core services for business logic and performance optimization.

Services are progressively moving into their domain apps. Import directly
from the owning app (e.g. ``from banking.services import BankingService``)
rather than relying on this re-export package.
"""

__all__: list[str] = []
