# Semgrep ignore file
# Files listed here are excluded from Semgrep analysis.
#
# admin_layout.html: The Semgrep django-no-csrf-token rule fires a false positive on the
# logout form because it cannot parse {% csrf_token %} in Django template syntax.
# The form is correctly protected — {% csrf_token %} is present inside the form element.
templates/layouts/admin_layout.html
